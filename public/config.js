// For a same-origin deploy (server serves /api), keep this as '/api'.
// For a separate frontend/backend deployment, change to the backend URL, e.g.
// window.FRAME_API = 'https://framie-api.example.com/api';
window.FRAME_API = '/api';
